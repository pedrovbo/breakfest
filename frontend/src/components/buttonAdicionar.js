import { render } from "@testing-library/react";
import styled from "styled-components";

export const AdicionarColaboradorButton = styled.button`
  margin-right: 2%;
  background-color: inherit;
  transition: all 0.3s;
  
  :hover {
    transform: scale(1.2);
  }
 `
export const AdicionarOpcaoCafeManhaButton = styled.button`
  background-color: inherit;
  transition: all 0.3s;
  
  :hover {
    transform: scale(1.2);
  }
`;
